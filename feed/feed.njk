---
eleventyExcludeFromCollections: true
permalink: "{{ metadata.feed.path }}"
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
	<title>{{ metadata.title }}</title>
	{% set absoluteUrl %}{{ metadata.feed.path | url | absoluteUrl(metadata.url) }}{% endset %}
	<link href="{{ absoluteUrl }}" rel="self"/>
	<link href="{{ metadata.url }}"/>
	<id>{{ metadata.feed.id }}</id>
	<subtitle>{{ metadata.description }}</subtitle>
	<updated>{{ collections.posts | rssLastUpdatedDate }}</updated>
	<author>
		<name>{{ metadata.author.name }}</name>
		<email>{{ metadata.author.email }}</email>
	</author>
	{%- for post in collections.posts | reverse %}
	{% set absolutePostUrl %}{{ post.url | url | absoluteUrl(metadata.url) }}{% endset %}
	<entry>
		<title>{{ post.data.title }} {% if not post.data.author %}by @j9t{% else %}by @{{ post.data.author }}{% if post.data.publisher %} (@{{ post.data.publisher }}){% endif %}{% endif %}</title>
		<link href="{% if post.data.target_url %}{{ post.data.target_url }}{% else %}{{ absolutePostUrl }}{% endif %}"/>
		<id>{{ absolutePostUrl }}</id>
		<content type="html">{% if post.templateContent %}{{ post.templateContent | htmlToAbsoluteUrls(absolutePostUrl) }}{% else %}<p>{{ metadata.description_fallback }}</p>{% endif %}</content>
		<updated>{{ post.date | rssDate }}</updated>
	</entry>
	{%- endfor %}
</feed>