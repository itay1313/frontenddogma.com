---
layout: home.njk
permalink: /
---
<p>This is Frontend Dogma.
<div>
	<h2>The Latest in Frontend Development</h2>
	{% set maxPosts = collections.posts.length %}
	<p>Last {% if maxPosts == 1 %}post{% else %}{{ maxPosts }} posts{% endif %}:
	{% set posts = collections.posts | head(-50) %}
	{% set postsCounter = collections.posts | length %}
	{% include "posts.njk" %}
	<p><a href={{ '/posts/' | url }}>All posts</a> · <a href={{ '/tags/' | url }}>All tags</a>
</div>
<div>
	<h2>Featured</h2>
	<ul>
	{% for featured in collections.featureList | reverse | head(-5) %}
		<li><a href={{ featured.url | url }}>{% if featured.data.title %}{{ featured.data.title }}{% else %}<code>{{ featured.url }}</code>{% endif %}</a>
	{% endfor %}
	</ul>
</div>
<div>
	<h2>You and Frontend Dogma</h2>
	<ul>
		<li class=status-green>Promote yourself: <a href=@@>add your developer links</a>
		<li class=status-green>Suggest content: <a href=@@>write a post</a>
		<li class=status-yellow>Publish guest content: upcoming
		<li class=status-red>Sponsor content: wait for it
		<li class=status-red>Get published: wait for it
	</ul>
</div>