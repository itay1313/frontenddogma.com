---
layout: base.njk
---
<article>
  <h1>{% if target_url %}<a href="{{ target_url }}">{% endif %}{{ title }}{% if target_url %}</a>{% endif %}</h1>
  {% if author or tags %}<p id=fd-meta>{% endif %}{% if author %}by <a href=https://twitter.com/{{ author }}>@{{ author }}</a> on {% endif %}{% if tags %}<time datetime={{ page.date | htmlDateString }}>{{ page.date | readableDate }}</time>, tagged {% set comma = joiner() %}{% for tag in tags | filterTagList %}{% set tagUrl %}/tags/{{ tag | slug }}/{% endset %}{{ comma() }} <a href={{ tagUrl | url }} rel=tag>{{ tag }}</a> {%- endfor %}<span> · <a href="https://twitter.com/intent/tweet?text=%E2%80%9C{{ title | urlencode }}%E2%80%9D{% if author %}%20by%20@{{ author }}{% endif %},%20featured%20on%20@FrontendDogma%3A%0A%0A{% if target_url %}{{ target_url }}{% else %}{{ metadata.url_truncated }}{{ page.url | url }}{% endif %}">Tweet this?</a></span>{% endif %}
  {% if image %}<div id=fd-teaser>{% if target_url %}<a href="{{ target_url }}">{% endif %}<!-- @@ Add CDN reference --><img src=/media{{ image }} alt="{{ title }}">{% if target_url %}</a>{% endif %}</div>{% endif %}

{{ content | safe }}

{% if target_url %}<p>(This post was published elsewhere. Check out <a href="{{ target_url }}">{{ title }}</a>!){% endif %}

{% if original_url %}<p>(This post was <a href="{{ original_url }}" rel=canonical>originally published elsewhere</a>.){% endif %}

{%- set nextPost = collections.posts | getNextCollectionItem(page) %}
{%- set previousPost = collections.posts | getPreviousCollectionItem(page) %}
{%- if nextPost or previousPost %}
  <ul class="fd-nav fd-alt">
    {% if nextPost %}<li>Next: <a href={{ nextPost.url | url }}>{{ nextPost.data.title }}</a></li>{% endif %}
    {% if previousPost %}<li>Previous: <a href={{ previousPost.url | url }}>{{ previousPost.data.title }}</a></li>{% endif %}
  </ul>
{%- endif %}
</article>